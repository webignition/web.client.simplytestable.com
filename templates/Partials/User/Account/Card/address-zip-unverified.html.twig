{% if (has_card and not is_passed_address_zip_check) or (has_card_zip_error) %}
    {% include 'Partials/alert.html.twig' with {
        type: 'error',
        view: 'Partials/User/Account/Card/Alert/Content/address-zip-unverified.html.twig'
    } only %}
{% endif %}

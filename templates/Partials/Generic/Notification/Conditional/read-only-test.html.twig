{% if is_owner == false and is_public_user_test == false %}
    {% include 'Partials/alert.html.twig' with {
        type: 'info',
        view: 'Partials/Alert/Content/read-only-test.html.twig',
        wrap_in_container: true,
        dismissable: false
    } only %}
{% endif %}

{% if (has_card and not is_passed_cvc_check) or (has_card_cvc_error) %}
    {% include '@SimplyTestableWebClient/bs3/Partials/alert.html.twig' with {
        type: 'error',
        view: 'Partials/User/Account/Card/Alert/Content/cvc-unverified.html.twig'
    } only %}
{% endif %}

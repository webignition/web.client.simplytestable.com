language: php
php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm
  
before_script:  
  # Install code quality tools
  #- wget http://pear.phpunit.de/get/phpcpd.phar
  
  # Install composer for dependency management
  - wget http://getcomposer.org/composer.phar   
  - chmod +x composer.phar
  
  # Set the GitHub OAuth token to make use of the 6000 per hour rate limit
  - "mkdir -p ~/.composer"
  - cp .travis.composer.config.json ~/.composer/config.json   
  
  # Build!
  #- mkdir app/cache/test  
  - mv app/config/parameters.yml.travis app/config/parameters.yml
  - mv --force app/config/config_travis.yml app/config/config_test.yml
  - cp src/SimplyTestable/WebClientBundle/Resources/config/parameters.yml.travis src/SimplyTestable/WebClientBundle/Resources/config/parameters.yml
  - rake
  - php app/console doctrine:database:create
  - php app/console doctrine:migrations:migrate --no-interaction
  
script:
  - phpunit -c app
  
#after_script:
#  - php phpcpd.phar --exclude src/SimplyTestable/WebClientBundle/Tests/ src/SimplyTestable/WebClientBundle/

notifications:
  irc:
    - "irc.freenode.org#simplytestable"

matrix:
  allow_failures:
    - php: 5.6
    - php: hhvm
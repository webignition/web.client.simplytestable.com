<div class="meta">
    {% if message.lineNumber > -1 %}
        <span class="text">Line</span>
        <span class="digit">{{ message.lineNumber }}</span>
    {% endif %}
</div>
<div class="content">
    {% if not message.context == ''%}
        <code>{{ message.context|raw }}</code><br>
    {% endif %}

    {#{{ message|split("\n")|join("<br>")|replace({"\t": '<span class="tab"></span>'})|raw }}#}

    {{ message|raw }}
</div>
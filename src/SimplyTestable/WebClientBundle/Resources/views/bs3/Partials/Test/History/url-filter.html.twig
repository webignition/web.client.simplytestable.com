<span class="badge action-badge action-badge-filter-options {% if filter is not empty %}action-badge-enabled{% endif %}">
    <i class="fa fa-filter icon-primary"></i>
    <span class="action-badge-label">{% if filter is empty %}Showing all results{% else %}Showing results for <strong>{{ filter }}</strong>{% endif %}</span>
    <span class="modal-control" data-for="filter-options-modal">
        filter by url
    </span>
</span>

<div class="modal" id="filter-options-modal">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h3>Filter by URL</h3>
                <p>
                Filter the test list to those made against a given URL.
                </p>
            </div>
            <div class="modal-body">
                <input name="filter"
                id="input-filter"
                class="form-control typeahead"
                type="text"
                placeholder="URL to filter by &hellip;"
                value="{{ filter }}"
                data-source-url="{{ websites_source }}"
                data-provider="typeahead">
                <input name="closed_by" type="hidden">
            </div>
            <div class="modal-footer">
                <button id="clear-filter-button" type="button" class="btn btn-default" data-dismiss="modal">Clear filter</button>
                <button id="apply-filter-button" type="button" class="btn btn-primary" data-dismiss="modal">Apply filter</button>
            </div>
        </div>
    </div>
</div>

{#<span class="badge action-badge action-badge-filter-options {% if filter is not empty %}action-badge-enabled{% endif %}">#}
    {#<i class="icon icon-filter icon-primary"></i>#}
    {#<span class="action-badge-label">{% if filter is empty %}Showing all results{% else %}Showing results for <strong>{{ filter }}</strong>{% endif %}</span>#}
    {#<span class="modal-control for-filter-options-modal">#}
        {#filter by url#}
    {#</span>#}
{#</span> #}

{#<div id="filter-options-modal" class="modal-content modal-type-filter-options">#}
    {#<div class="modal-options">#}
        {#<span class="action-label">Apply filter</span>#}
    {#</div>#}

    {#<div class="modal-header-content">#}
        {#<h3>Filter by URL</h3>#}
        {#<span class="help-block modal-transform modal-transform-p">#}
            {#Filter the test list to those made against a given URL.#}
        {#</span>           #}
    {#</div>#}

    {#<div class="modal-body-content">            #}
        {#<input name="filter"                   #}
               {#id="input-filter"#}
               {#class="input-block-level"#}
               {#type="text"#}
               {#placeholder="URL to filter by &hellip;"#}
               {#value="{{ filter }}"#}
               {#data-source-url="{{ websites_source }}"#}
               {#>#}


    {#</div>#}

    {#<div class="modal-footer-content">#}
        {#<button id="clear-filter-button" class="btn" data-dismiss="modal" aria-hidden="true">Clear filter</button>#}
    {#</div>#}
{#</div>#}
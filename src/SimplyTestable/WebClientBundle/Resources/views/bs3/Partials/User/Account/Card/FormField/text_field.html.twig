<input type="text"
       class="form-control"
       id="{{ field_id }}"
       placeholder="{{ placeholder }}"
       data-stripe="{{ stripe_name }}"
       value="{{ value }}"
       {{ field_attributes }}
       >

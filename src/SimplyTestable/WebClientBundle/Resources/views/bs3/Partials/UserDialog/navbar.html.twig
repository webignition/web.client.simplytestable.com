{% set username_parts = user.username|split('@') %}    

<div class="navbar navbar-inverse" role="navigation">
    <div class="container col-xs-12">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>       

            <a class="navbar-brand" href="{{ public_site.urls.home }}">
                SimplyTestable                     
            </a>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
                <li class="text hidden-lg hidden-md hidden-sm {% if app.request.get('_route') == 'app' %}active{% endif %}">
                    <a href="{{ path('app') }}">
                        <i class="fa fa-tachometer"></i>
                        {% if is_logged_in %}
                            Dashboard
                        {% else %}
                            Demo <span class="hidden-sm hidden-md">dashboard</span> (try me)
                        {% endif %}                                
                    </a>
                </li>
                <li class="text hidden-lg hidden-md hidden-sm">
                    <a href="{{ public_site['urls']['home'] }}{{ public_site['urls']['features'] }}">
                        <i class="fa fa-signal"></i> Features
                    </a>
                </li>
                <li class="text hidden-lg hidden-md hidden-sm">
                    <a href="{{ public_site['urls']['home'] }}{{ public_site['urls']['plans_and_pricing'] }}">
                        <i class="fa fa-calendar"></i> Pricing <span class="hidden-sm">and plans</span>
                    </a>
                </li>

{#                {% if is_logged_in %}
                    <li class="user-actions text">
                        <p class="navbar-text spacer highlighted-spacer hidden-xs">&nbsp;</p>
                        <a class="username" href="{{ path('user_account_index') }}">
                            <i class="fa fa-user"></i> {{ username_parts[0] }}<span class="hidden-sm">@{{username_parts[1]}}</span>
                        </a>
                    </li>                         
                    <li>                    
                        <form class="navbar-form signout-form" method="post" action="{{ path('sign_out_submit') }}">
                            <button type="submit" class="btn btn-primary btn-sm">
                                Sign out
                                <i class="fa fa-caret-right"></i>
                            </button>
                        </form>
                    </li>                               
                {% else %}#}
                    <li class="user-actions text">
{#                        <p class="navbar-text spacer highlighted-spacer hidden-xs">&nbsp;</p>#}
                        <a class="username" href="{{ path('sign_in') }}">
                            <i class="fa fa-sign-in"></i> Sign in
                        </a>
                    </li>                                        
                    <li class="text {% if app.request.get('_route') == 'sign_up' %}active{% endif %}">
{#                        <p class="navbar-text spacer highlighted-spacer hidden-xs">&nbsp;</p>#}
                        <a class="" href="{{ path('sign_up') }}">
                            <i class="fa fa-user"></i> Create an account
                        </a>
                    </li>                                               
{#                {% endif %}#}
            </ul>            
        </div><!--/.navbar-collapse -->
    </div>
</div>
{% extends 'SimplyTestableWebClientBundle::bs3/Abstract/single-page-dialog.html.twig' %}

{% block title %}Sign up - {{ parent() }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}

    {% stylesheets    
    '@SimplyTestableWebClientBundle/Resources/assets/bs3/css/sign-up.css'
    filter='cssrewrite' %}
    <link rel="stylesheet" href="{{ asset_url }}" />
    {% endstylesheets %}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    {% javascripts
        '@SimplyTestableWebClientBundle/Resources/assets/bs3/js/jQuery/stFormErrorHelper.js'
        '@SimplyTestableWebClientBundle/Resources/assets/bs3/js/jQuery/stFormHelper.js'
        '@SimplyTestableWebClientBundle/Resources/assets/bs3/js/sign-up.js'
        '@SimplyTestableWebClientBundle/Resources/assets/bs3/js/form-helper.js'
    %}
    <script type="text/javascript" src="{{ asset_url }}"></script>
    {% endjavascripts %} 
{% endblock %}

{#{% block nav %}
    {% include 'SimplyTestableWebClientBundle:bs3/Partials/UserDialog:navbar.html.twig' %}
{% endblock %}    #}

{% block content %}

    <h1>Create your free account</h1>
    
    <p>
        Private tests, additional types of test, your own test history.
        <a class="account-benefits-link" href="{{ [public_site.urls.home,public_site.urls.account_benefits]|join('') }}">Account benefits <i class="fa fa-caret-right"></i></a>
    </p>
{#    <p>
        Keep track of the tests you've started, find past test
        results, easily re-test when you make changes.
    </p>#}
    <p>
        30-day free trial on all premium plans.
{#        Choose any premium plan free for the first 30 days.#}
    </p>

    <form role="form" method="post" action="{{ path('sign_up_submit') }}" novalidate>     

        {% if user_create_error is defined and user_create_error != '' %}
            {% if user_create_error == 'blank-email' %}
                <div class="alert alert-danger for:email" data-for="email">
                    <button type="button" class="close" data-dismiss="alert">×</button>
                    <strong>Slow down!</strong> You're going to have to enter in your email address to sign up.
                </div>                
            {% endif %}

            {% if user_create_error == 'blank-password' %}
                <div class="alert alert-danger">
                    <button type="button" class="close" data-dismiss="alert">×</button>
                    <strong>Slow down!</strong> You're going to have to choose a password for your account.
                </div>                
            {% endif %}        

            {% if user_create_error == 'invalid-email' %}
                <div class="alert alert-danger" data-for="email">
                    <button type="button" class="close" data-dismiss="alert">×</button>
                    <strong>Hmmm.</strong> I'm not convinced that <strong>{{ email }}</strong> is a valid email address.
                </div>                
            {% endif %}                

            {% if user_create_error == 'create-failed' %}
                <div class="alert alert-danger" data-for="email">
                    <button type="button" class="close" data-dismiss="alert">×</button>
                    <strong>Something went wrong.</strong> I tried to sign up <strong>{{ email }}</strong> but it didn't
                    work. I don't know why.
                </div>                
            {% endif %}                                                    

            {% if user_create_error == 'create-failed-read-only' %}
                <div class="alert alert-danger for:email" data-for="email">
                    <button type="button" class="close" data-dismiss="alert">×</button>
                    <p><strong>Sorry, I can't do that right now</strong></p>
                    <p>
                        We're in a <strong>read-only</strong> state whilst some 
                        <strong>maintenance</strong> is going on.
                    </p>
                    <p>
                        Give it a go again soon, this won't take long.
                    </p>
                </div>                
            {% endif %}         
        {% endif %}        

        <div class="form-group-collection">
            <div class="form-group">
                <label for="email">Email address</label>
                <input name="email" type="email" class="form-control input-larger" id="email" placeholder="Enter your email address" {% if email != '' %} value="{{ email }}" {% endif %}>
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <input name="password" type="password" class="form-control input-larger" id="password" placeholder="Choose a password">
            </div>
        </div>  
        <div class="form-group-collection">
            <div class="form-group">
                <label for="password">Account plan</label> <a class="hidden-sm hidden-md hidden-lg" href="{{ public_site['urls']['home'] }}{{ public_site['urls']['plans_and_pricing'] }}">Plans and pricing <i class="fa fa-caret-right"></i></a>

                <div class="row match-height">
                    <div class="col-sm-6 col-md-6">
                        <select class="form-control" name="plan" id="plan">
                            <option value="basic" {% if plan == 'basic' %}selected{% endif %}>Basic (free)</option>
                            <option value="personal" {% if plan == 'personal' %}selected{% endif %}>Personal (&pound;9 per month)</option>
                            <option value="agency" {% if plan == 'agency' %}selected{% endif %}>Agency (&pound;19 per month)</option>
                            <option value="business" {% if plan == 'business' %}selected{% endif %}>Business (&pound;59 per month)</option>
                        </select>                    
                    </div>
                    <div class="hidden-xs col-sm-6 col-md-6 plans-link">
                        <a href="{{ public_site['urls']['home'] }}{{ public_site['urls']['plans_and_pricing'] }}">Plans and pricing <i class="fa fa-caret-right"></i></a>
                    </div>    
                </div>
            </div>
        </div>

        <div class="form-group-collection form-group-submit-collection">            
            <button id="submit" type="submit" class="btn btn btn-primary">Sign up!</button>

            <p class="terms-notice additional">
                By making use of this website you agree to the <a href="{{ external_links.terms }}">terms of service</a>.
            </p>  
        </div>

    </form>


    {#<form class="form-horizontal" method="post" action="{{ path('sign_up_submit') }}">            
            {% if user_create_error is defined and user_create_error != '' %}
                {% if user_create_error == 'blank-email' %}
            <div class="alert alert-danger for:email" data-for="email">
                <button type="button" class="close" data-dismiss="alert">×</button>
                <strong>Slow down!</strong> You're going to have to enter in your email address to sign up.
            </div>                
                {% endif %}
            
                {% if user_create_error == 'blank-password' %}
            <div class="alert alert-danger for:input-password">
                <button type="button" class="close" data-dismiss="alert">×</button>
                <strong>Slow down!</strong> You're going to have to choose a password for your account.
            </div>                
                {% endif %}        
    
                {% if user_create_error == 'invalid-email' %}
            <div class="alert alert-danger for:email" data-for="email">
                <button type="button" class="close" data-dismiss="alert">×</button>
                <strong>Hmmm.</strong> I'm not convinced that <strong>{{ email }}</strong> is a valid email address.
            </div>                
                {% endif %}                
    
                {% if user_create_error == 'create-failed' %}
            <div class="alert alert-danger for:email" data-for="email">
                <button type="button" class="close" data-dismiss="alert">×</button>
                <strong>Something went wrong.</strong> I tried to sign up <strong>{{ email }}</strong> but it didn't
                work. I don't know why.
            </div>                
                {% endif %}                                                    
            
                {% if user_create_error == 'create-failed-read-only' %}
            <div class="alert alert-danger for:email" data-for="email">
                <button type="button" class="close" data-dismiss="alert">×</button>
                <p><strong>Sorry, I can't do that right now</strong></p>
                <p>
                    We're in a <strong>read-only</strong> state whilst some 
                    <strong>maintenance</strong> is going on.
                </p>
                <p>
                    Give it a go again soon, this won't take long.
                </p>
            </div>                
                {% endif %}         
            {% endif %}
    
            {% if user_create_confirmation is defined and user_create_confirmation != '' %}
                {% if user_create_confirmation == 'user-exists' %}
            <div class="alert alert-success for:email" data-for="email">
                <button type="button" class="close" data-dismiss="alert">×</button>
                <p><strong>Guess what?</strong> Looks like <strong>{{ email }}</strong>
                    already has an account.</p>
                <p>
                    Do you need to <a href="{{ path('reset_password', {'email': email}) }}">reset your password</a>?                
                </p>
            </div>                
                {% endif %}                         
            {% endif %}
    
            <div class="larger">
                <label for="email">Email</label>
                <input type="text" id="email" name="email" placeholder="Enter your email address" {% if email != '' %} value="{{ email }}" {% endif %}>
            </div>
            <div class="larger">
                <label  for="password">Password</label>
                <input name="password" type="password" id="password" placeholder="Choose a password" />
            </div>
            <div class="larger">
                <label  for="password">Account plan</label>
                <select name="plan">
                    <option value="basic" {% if plan == 'basic' %}selected{% endif %}>Basic (free)</option>
                    <option value="personal" {% if plan == 'personal' %}selected{% endif %}>Personal (&pound;9 per month)</option>
                    <option value="agency" {% if plan == 'agency' %}selected{% endif %}>Agency (&pound;19 per month)</option>
                    <option value="business" {% if plan == 'business' %}selected{% endif %}>Business (&pound;59 per month)</option>              
                    <option value="enterprise" {% if plan == 'enterprise' %}selected{% endif %}>Enterprise (&pound;299 per month)</option>
</select>
<a href="{{ public_site['urls']['home'] }}{{ public_site['urls']['plans_and_pricing'] }}">View plan and pricing details <i class="icon icon-caret-right"></i></a>    
</div>        
<div>
    <button class="submit btn btn-large btn-primary" type="submit">Sign up!</button>
    <p class="terms-notice">
        By making use of this website you agree to the <a href="http://help.simplytestable.com/legal/terms-of-service/">terms of service</a>.
    </p>
</div>
</form>  #} 

{% endblock %}
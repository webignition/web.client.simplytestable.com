{% if plural_suffix is not defined %}
    {% set plural_suffix = 's' %}
{% endif %}

{% if positive is not defined %}
    {% set positive = 'success' %}
{% endif %}

{% if negative is not defined %}
    {% set negative = 'danger' %}
{% endif %}

{% if name_suffix is not defined %}
    {% set name_suffix = '' %}
{% endif %}

{% if anchor is not defined %}
    {%  set anchor = '#' %}
{% endif %}

{% if stacked is not defined %}
    {% set stacked = false %}
{% endif %}

{% if count == 0 %}
<span class="{% if stacked %}row{% endif %} label label-{{ count == 0 ? positive : negative }}">
    <span class="{% if stacked %}col-md-12 col-sm-6 col-xs-6{% endif %} count">{{ count|number_format }}</span>
    <span class="{% if stacked %}col-md-12 col-sm-6 col-xs-6{% endif %} name">{{ name }}{% if count != 1 %}{{ plural_suffix }}{% endif %}{{ name_suffix }}</span>
    <span class="icon">
        {% if count == 0 %}
            <i class="fa fa-check"></i>
        {% else %}
            <i class="fa fa-caret-down"></i>
        {% endif %}
    </span>
</span>
{% else %}
    <a data-target="#test-list" data-target="#test-list" href="{{ anchor }}" class="{% if stacked %}row{% endif %} label label-{{ count == 0 ? positive : negative }}">
        <span class="{% if stacked %}col-md-12 col-sm-6 col-xs-6{% endif %} count">{{ count|number_format }}</span>
        <span class="{% if stacked %}col-md-12 col-sm-6 col-xs-6{% endif %} name">{{ name }}{% if count != 1 %}{{ plural_suffix }}{% endif %}{{ name_suffix }}</span>
        <span class="icon">
            <i class="fa fa-caret-down"></i>
        </span>
    </a>
{% endif %}




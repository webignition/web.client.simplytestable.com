<div class="navbar navbar-inverse navbar-fixed-top">    
    <div class="navbar-inner">

        <div class="container">
            <div class="row-fluid">
                <div class="span12">
                    <a class="brand" href="{{ public_site['urls']['home'] }}">
                        SimplyTestable
                    </a>
                    <ul class="nav">
                        <li class="active"><i class="icon icon-play-circle"></i> <a href="{{ path('app') }}">Start a new website test</a></li>
                        <!--
                        <li><a href="#">Link</a></li>
                        <li><a href="#">Link</a></li>
                        -->
                    </ul> 
                    {% if is_logged_in %}
                    <p class="navbar-text pull-right">
                        Logged in as <a href="#" class="navbar-link">{{ user.username }}</a>
                    </p>                        
                    {% else %}
                    <p class="navbar-text pull-right">
                        <a href="{{ path('sign_in') }}" class="navbar-link"><i class="icon icon-user"></i> Sign in</a>
                    </p>                    
                    {% endif %}                       
                </div>
            </div>
        </div>

    </div>
</div>
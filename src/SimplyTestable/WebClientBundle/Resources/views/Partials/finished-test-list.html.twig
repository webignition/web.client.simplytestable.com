{% if test_list.length == 0 %}                       
    <p class="info">
        {% if is_logged_in %}
        None of your tests have finished running. When 
        they have, you'll see a summary of your recent tests here.
        {% else %}
        No demo tests have finished running. When 
        they have, you'll see a summary of your recent tests here.
        {% endif %}
    </p>
{% else %}
    {% for test in test_list.get %}
        {% include 'SimplyTestableWebClientBundle:Partials:finished-job-summary.html.twig' %}
    {% endfor %}
{% endif %} 
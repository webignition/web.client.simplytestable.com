{% if is_logged_in %}
    This test has been limited to only the first <strong>{{remote_test_summary.crawl.limit}}</strong>
    URLs that were discovered.
    <a href="{{ path('user_account_index') }}">Upgrade your plan</a> to allow larger sites to be tested. 
{% else %}
    This <strong>free demo</strong> has been limited to only the first <strong>{{remote_test_summary.crawl.limit}}</strong>
    URLs that were discovered.
    <a href="{{ path('sign_up') }}">Create an account</a> or <a href="{{ path('sign_in') }}">sign in</a> to start a new test without this limit.
{% endif %}